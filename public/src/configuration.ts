export const WEB_SOCKET_SERVER_URL = `wss://${window.location.hostname}:7777/ws`;
export const HTTP_SERVER_URL = `http://${window.location.hostname}:${window.location.port === '3000' ? '8000' : window.location.port}`;
